# 7 · Paralog redundancy / divergence
Embedding-similarity prediction of paralog redundancy, with AA-sequence distance (`AAsequenceDistance/`), weighted-PCC distance (`wPCC_Distance/`), and a Spearman random control (`Random_SCC_DB/`). Main: `Fig_Paralogs.v2.R`.
